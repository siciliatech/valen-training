generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Item {
  id          Int    @id @default(autoincrement())
  name        String
  description String
}
